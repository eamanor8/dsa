#include <iostream>

/*
    SPARSE MATRIX
    Is a matrix that has majority of it's entries as zeroes. 
    The non-zero values can be represented as a tuple of 3-values consisting of
    (row, col, value)
*/

int main(){

    return 0;
}